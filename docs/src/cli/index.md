# CLI Reference

Complete reference for every command in AgentForge (the `agentforge` binary; the legacy `agentforge` alias is also available).

::: mkdocs-click
    :module: google.agents.cli.main
    :command: main
    :prog_name: agentforge
    :depth: 2
    :show_subcommand_aliases: true
